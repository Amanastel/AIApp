const initalState = {
  isLoading: false,
  isError: false,
  data: [],
};

export const reducer = (state = initalState, { type, payload }) => {
  switch (type) {
    default: {
      return state;
    }
  }
};
